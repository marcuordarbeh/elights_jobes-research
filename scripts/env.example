# /home/inno/elights_jobes-research/scripts/env.example
# Example environment variables for elights_jobes-research
# Copy this file to .env in the project root and fill in actual values.
# Do NOT commit the .env file with real secrets.

# === Database Configuration ===
POSTGRES_USER=core_user
POSTGRES_PASSWORD=securepassword
POSTGRES_DB=core_db
POSTGRES_HOST=postgres # Service name in docker-compose
POSTGRES_PORT=5432
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# === Core API Service ===
CORE_API_PORT=8080
API_BIND_ADDR=0.0.0.0:${CORE_API_PORT}
JWT_SECRET=generate_a_very_strong_random_secret_here # Use a secure random string

# === Bank Server TLS (If core-api listens on TLS directly) ===
BANK_HOST=0.0.0.0 # Or specific IP
BANK_PORT=8443
BANK_SERVER_CERT=./config/certs/cert.pem # Example path relative to where API runs
BANK_SERVER_KEY=./config/certs/key.pem  # Example path relative to where API runs

# === Bank Integrations ===
# USA
WELLS_FARGO_API_BASE=https://api.wellsfargo.com/v1
WELLS_FARGO_API_KEY=your_wf_api_key
CHASE_API_BASE=https://api.chase.com/v1
CHASE_API_KEY=your_chase_api_key
JPMORGAN_API_BASE=https://api.jpmorgan.com/v1
JPMORGAN_API_KEY=your_jpm_api_key
# Europe
BNP_PARIBAS_API_BASE=https://psd2.api.cib.bnpparibas.com
BNP_PARIBAS_OAUTH_TOKEN=your_bnp_token
DEUTSCHE_BANK_API_BASE=https://simulator-api.db.com/gw/dbapi
DEUTSCHE_BANK_CLIENT_ID=your_db_client_id
DEUTSCHE_BANK_CLIENT_SECRET=your_db_secret
SANTANDER_API_BASE=https://api.santander.co.uk
SANTANDER_OAUTH_TOKEN=your_santander_token

# === Cryptography Exchange ===
# BTCPay Server
BTCPAY_URL=http://btcpay:23000 # Internal Docker network URL
BTCPAY_API_KEY=your_btcpay_api_key
# Monero Wallet RPC
MONERO_WALLET_RPC_PORT=18082
MONERO_WALLET_RPC=http://monero-wallet:${MONERO_WALLET_RPC_PORT} # Internal Docker network URL
MONERO_WALLET_USER=rpc_user
MONERO_WALLET_PASSWORD=rpc_password

# === Tor Network ===
TOR_SOCKS_PORT=9050
TOR_CONTROL_PORT=9051
TOR_PASSWORD=your_tor_password # Use a strong password

# === Other Services ===
# FT Asset Management
FT_ASSET_URL=https://my.ftassetmanagement.com
FT_ASSET_ALLOWED_IPS=203.0.113.5,203.0.113.6 # Example IPs
# FT_ASSET_CLIENT_CERT=path/to/ft_client.pem # Optional paths
# FT_ASSET_CLIENT_KEY=path/to/ft_client.key  # Optional paths

# === Logging ===
RUST_LOG=info # Log level (e.g., info, debug, warn, error)